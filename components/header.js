import styles from 'styles/header.module.css'

const Header = () => {
  return (
    <header className={styles.header}>
      <p className={styles.title}>RANDOMFOX API
      </p>
    </header>
  )
}
export default Header
